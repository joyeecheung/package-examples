
//! [logger_start]
class Logger {
  // ... Logger implementation
//! [logger_start]
  constructor(level) {
    this.level = level;
  }

  log(message) {
    console.log(`[${this.level}] ${message}`);
  }
//! [logger_end]
};
module.exports = {
  Logger,  // This can be detected as a named export
};
//! [logger_end]
